## Что делаем

Полностью перезаписываем два файла содержимым из приложенных версий 15 апреля:

- `src/pages/Promo.tsx` ← содержимое `Promo.tsx` (241 строка)
- `src/pages/PromoThanks.tsx` ← содержимое `PromoThanks.tsx` (87 строк)

Остальные файлы (`YandexMetrika.tsx`, `vite.config.ts`, `ThanksScreen.tsx`, другие страницы) **не трогаем**.

## Технические детали

- `Promo.tsx`: вернётся к версии без UTM-проброса в `goToThanks` (просто `navigate("/promo/thanks")`), без gradient hero-badge и social proof из редизайна 27 мая.
- `PromoThanks.tsx`: вернётся к самописной верстке с таймером 10 сек и кнопкой регистрации, **без** использования общего компонента `ThanksScreen`.
- Цели Я.Метрики (`promo_loaded`, `promo_thanks_loaded`) продолжат отправляться централизованно через `YandexMetrika.tsx` — это работает независимо от содержимого страниц.
- Маршруты в `App.tsx` остаются прежними — менять не нужно.

## После применения

Проверим, что страницы открываются и трекинг целей в Метрике продолжает работать.