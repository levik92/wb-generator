В `src/components/dashboard/AdminAnalyticsChart.tsx` (строка 654) у кнопки «Подробнее» поменять класс `rounded-xl` на `rounded-md`, чтобы кнопка стала квадратной со слегка скруглёнными углами (размер `h-10 w-10` оставляем).